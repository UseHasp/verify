{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://usehasp.com/schema/audit-export/v1.0.json",
  "title": "Hasp audit export, schema_version 1.0",
  "description": "Authoritative shape check is in src/checks/schema.js; this document mirrors it for non-JS verifiers.",
  "type": "object",
  "required": ["schema_version", "export", "verification", "entries"],
  "additionalProperties": true,
  "properties": {
    "schema_version": { "const": "1.0" },
    "export": {
      "type": "object",
      "required": ["tenant", "tenant_id", "range", "exported_at", "exported_by", "entry_count"],
      "additionalProperties": true,
      "properties": {
        "tenant": { "type": "string" },
        "tenant_id": { "type": "string", "minLength": 1 },
        "range": {
          "type": "object",
          "required": ["from", "to"],
          "additionalProperties": true,
          "properties": {
            "from": { "type": "string" },
            "to": { "type": "string" }
          }
        },
        "exported_at": { "type": "string" },
        "exported_by": { "type": "string" },
        "entry_count": { "type": "number" }
      }
    },
    "verification": {
      "type": "object",
      "required": [
        "algo",
        "public_key_pem",
        "key_id",
        "key_published_at",
        "chain_head_hash",
        "tsa_anchor_chain"
      ],
      "additionalProperties": true,
      "properties": {
        "algo": { "const": "ed25519" },
        "public_key_pem": { "type": "string", "pattern": "BEGIN PUBLIC KEY" },
        "key_id": { "type": "string", "minLength": 1 },
        "keys_url": { "type": "string", "format": "uri", "pattern": "^https://" },
        "key_published_at": { "type": "string" },
        "chain_head_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "tsa_anchor_chain": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": [
              "checkpoint_after_entry",
              "tsa_url",
              "tsa_cacert_url",
              "tsa_tsr_base64",
              "anchored_data"
            ],
            "additionalProperties": true,
            "properties": {
              "checkpoint_after_entry": { "type": "integer", "minimum": 1 },
              "tsa_url": { "type": "string", "format": "uri", "pattern": "^https://" },
              "tsa_cacert_url": { "type": "string", "format": "uri", "pattern": "^https://" },
              "tsa_tsr_base64": { "type": "string", "minLength": 1 },
              "anchored_data": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
            }
          }
        }
      }
    },
    "entries": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "user_id",
          "org_id",
          "project_id",
          "action",
          "entity_type",
          "entity_id",
          "metadata",
          "ip_address",
          "created_at",
          "phi_disposition",
          "subject_type",
          "subject_id_hmac",
          "prev_hash",
          "hash",
          "signature"
        ],
        "additionalProperties": true,
        "properties": {
          "user_id": { "type": ["integer", "string", "null"] },
          "org_id": { "type": ["integer", "string", "null"] },
          "project_id": { "type": ["integer", "string", "null"] },
          "action": { "type": "string", "minLength": 1 },
          "entity_type": { "type": ["string", "null"] },
          "entity_id": { "type": ["integer", "string", "null"] },
          "metadata": { "type": ["object", "array", "null"] },
          "ip_address": { "type": ["string", "null"] },
          "created_at": { "type": "string", "minLength": 1 },
          "phi_disposition": { "type": ["string", "null"] },
          "subject_type": { "type": ["string", "null"] },
          "subject_id_hmac": { "type": ["string", "null"] },
          "prev_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
          "signature": { "type": "string", "pattern": "^ed25519:" }
        }
      }
    }
  }
}
